<nav class="navbar navbar-expand-lg navbar-dark bg-primary mb-3">
    <div class="container-fluid">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link" href="{{  path('fishing_index')}}">Enregistrer un nouvelle pêche</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="https://www.peche35.fr/" target="_blank">Maison de la pêche</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link"
                            href="https://www.generationpeche.fr/181-les-fiches-poissons.htm"
                            target="_blank">Fiches poissons
                        </a>
                    </li>
                </ul>
                <ul class="navbar-nav ms-auto">
                    {# <li class="nav-item">
                        <a class="nav-link" href="{{  path('app_login')}}">Se connecter</a>
                    </li> #}
                    {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}">Se déconnecter</a>
                    </li>
                    {% endif %}
                </ul>
            </div>
        </div>
    </nav>