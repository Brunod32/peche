{% extends 'base.html.twig' %}

{% block title %}Nouvelle pêche{% endblock %}

{% block body %}
    {{ include('_navbar.html.twig') }}

    <div class="container-fluid">
            <div class="row">
                <div class="col-lg-12">
                    <h1 class="page-header text-center mb-5 mt-2">Enregistrer une nouvelle pêche</h1>
                </div>
            </div>
    
        <div class="m-5">
            {{ include('fishing/_form.html.twig') }}

            <a href="{{ path('fishing_index') }}" role="button" class="btn btn-success mt-2">Retour à la liste</a>
        </div>
    </div>

    {{ include('_footer.html.twig') }}
{% endblock %}
